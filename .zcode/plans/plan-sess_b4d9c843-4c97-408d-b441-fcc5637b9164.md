Six UI/flow changes to the MiTea storefront. Note: per AGENTS.md, before coding I'll check the relevant guides in `node_modules/next/dist/docs/` (Next 16.3.4 has breaking changes) — though all edits are inside existing client components, so API surface is minimal.

## 1. Hide "CATERING & EVENTS 0" chip (keep everything else)
`src/components/CategoryNav.tsx` (chip rendered ~lines 141-170 with `{cat.count}` badge; the category has count 0 in both static `src/data/menu-data.ts` and the live `/api/menu/categories` counts):
- Map over `categories.filter(cat => cat.count > 0)` so zero-count pills never render.
- Decouple the amber "Plan Event" button from the catering chip loop iteration (currently gated on `isCatering` inside the map) and render it once after the loop so it stays visible.
- Keep the homepage Catering section, "Plan Your Event" banner, and header Catering button untouched.

## 2. Remove the tagline
`src/app/page.tsx`:
- Line 90: remove the `description` value from the "Most Popular & House Specialties" section.
- Lines 230-232: guard the `<p>` with `{section.description && ...}` so no empty paragraph/extra spacing remains.

## 3. Remove two mobile-drawer (sidebar) entries
`src/components/Header.tsx`:
- Delete the "Send a Gift Drink" button (lines 347-356). Desktop "Gift" nav button stays, so `openSendGiftModal` and the `Gift` import remain.
- Delete the bottom "Order Pickup" CTA (lines 387-394); then remove the now-unused `MapPin` import and `openLocationModal` from the `useOrder()` destructure (verify no other usage in the file first).

## 4. Product card: clickable name + "Add to Cart" label
`src/components/ProductCard.tsx`:
- Product name (lines 292-294): wrap in `<h3><button type="button" onClick={handleAction} disabled={isUnavailable}>…</button></h3>` with link-like hover styling — clicking the name does exactly what pressing the action button does (opens the simplified order dialog for customizable items, direct-adds for snacks/desserts). No new pages.
- Button (lines 318-331): rename "Customize" → "Add to Cart", swap the `SlidersHorizontal` icon for a cart/plus icon, keep `onClick={handleAction}` and the disabled state. Remove the unused `SlidersHorizontal` import.
- Direct-add branches (lines 23-39 catering, 43-57 non-customizable): set `sugar: ""`, `ice: ""`, `toppings: []`.

## 5. Simplify ordering: keep Size, drop sugar/ice/toppings, add free-text box
- `src/components/ProductModal.tsx`: remove the Sweetness (177-204), Ice Level (206-233), and Toppings (235-281) sections plus their state (`selectedSugar/selectedIce/selectedToppings`) and `toppingsPrice`; keep Size and quantity stepper; add a "Special instructions" textarea (state `notes`, ~200 char max, placeholder like "e.g., less sweet, no ice, extra boba, allergies…"). `handleAdd` sends `sugar: ""`, `ice: ""`, `toppings: []`, `notes`. Rename footer button "Add to Order" → "Add to Cart" for consistency.
- `src/data/menu-data.ts`: add `notes?: string` to `CartItem`.
- Display surfaces — replace sugar/ice with notes where relevant: `CartDrawer.tsx:142`, `CheckoutModal.tsx:409` (and add `notes` to the order payload at ~133-134), `ConfirmationModal.tsx:220`, `OrderHistoryModal.tsx:422-423`, `admin/AdminOrders.tsx:395-396` (add a "Notes:" line so staff see requests).
- Backend: `src/lib/validators/order.ts` already accepts per-item `notes`. Thread `notes` through `src/lib/order-service.ts` (recalculatedItems shape + push, `order_items` insert rows, frontendPlacedOrder mapping, getUserOrders mapping), `src/lib/email.ts` (~162), and `src/app/api/payments/checkout/route.ts` (~69) — those sugar/ice lines are already conditional and become no-ops.
- Database: new migration `supabase/migrations/20260917_order_items_notes.sql` with `ALTER TABLE public.order_items ADD COLUMN IF NOT EXISTS notes TEXT;` and add `notes` to `order_items` in `src/types/database.types.ts` (hand-edited to match generated style).

## 6. Card image cover area
- Run the dev server and inspect the menu grid in the browser. The card image container is a fixed `h-48` (192px) with `object-cover` (`ProductCard.tsx:252-261`); the editmysite CDN product shots get cropped at this ratio on desktop. Adjust to a responsive aspect-ratio container (likely `aspect-[4/3]`, keeping `object-cover`, switching to `object-contain` only if inspection shows bad letterboxing), and match the loading skeleton (`h-44`) accordingly. Verify at mobile and desktop widths.

## Verification
- `npm run dev` + browser walkthrough: no "0" chip (Plan Event still present), tagline gone, mobile drawer without the two removed entries, name click opens the dialog, dialog shows only Size + notes, full add-to-cart → cart drawer → checkout summary flow, images framed correctly.
- `npx tsc --noEmit` for types.
- Note: the Supabase migration needs applying (`supabase db push` or manual SQL) before notes persist; without it orders still succeed since the insert tolerates the extra column only after it exists — I'll flag if the local DB isn't reachable.